package RamdomPractice1;
/*
public class PetWiki 
{
	
	import org.openqa.selenium.*;
	import java.io.*;
import java.time.Duration;

import org.openqa.selenium.chrome.*;
import org.openqa.selenium.support.ui.ExpectedConditions;
import org.openqa.selenium.support.ui.WebDriverWait;

	 /*   public static String getLoadedData(WebDriver driver) 
	    {
	        
	        driver = new ChromeDriver();
	        
	        driver.get("URL");
	        
	        WebElement button = driver.findElement(By.id("load-button"));
	        
	        button.click();
	        
	        WebDriverWait mywait = new WebDriverWait(driver,Duration.ofSeconds(1));
	        WebElement reenablebutton = mywait.until(ExpectedConditions.elementToBeClickable(button));
	        
	        if(reenablebutton.isEnabled())
	        {
	        	WebElement content = driver.findElement(By.id("content"));
	        	String s = content.getText();
	        	return s;
	        }
	        
	        else
	        {
	        	return null;
	        }
	    }
	}

}
*/